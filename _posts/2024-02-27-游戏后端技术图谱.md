---
layout: post
title: 游戏后端技术图谱
date: 2024-02-27 16:37:23 +0800
category: Game
---


# 游戏后端技术图谱

## 1.计算机网络

>   Http消息通信
>
>   Websocket消息通信
>
>   IO处理： 同步和异步IO

## 2.数据库技术

>   MySQL数据处理-ORM框架的使用
>
>   Redis读写

## 3.配置文件读取

>   excel, JSON, txt, TOML文件的读取和转化

## 4.进程和线程

>   线程模型
>
>   业务单线程，IO多线程

## 5.服务器部署

>   Linux命令
>
>   Python脚本
>
>   硬盘命令
>
>   tail, ls

## 6.服务器开发

>   game游戏服务器
>
>   robot机器人服务器
>
>   common公共库

## 7.单元测试

```
type GameTest struct {
	suite.Suite
	*GameBase
}

func (self *GameTest) SetupTest() {
	self.GameBase = &GameBase{}
	self.InitBase()
	
}

func TestGameModuleTest(t *testing.T) {
	suite.Run(t, new(GameTest))
}
 
// TestAction 进行业务测试
func (self *GameTest) TestAction() {
	
}
```

## 8.Http 接口测试

```
func Post(t *testing.T, router string, loginReq any, env int32) {
	client := &http.Client{}
	url := ""
	switch env {
	case 1:
		url = "http://127.0.0.1:1234/game/" + router
	case 2:
		url = "http://192.168.0.251:1234/game/" + router
	default:
	}
	
	lastMsg := strings.NewReader(resultStr)
	res, err := client.Post(url, "application/json", lastMsg)
	if err != nil {
		t.Errorf("expected nil response err:%q", err.Error())
	}
	body, err := io.ReadAll(res.Body)
	if err != nil {
		t.Errorf("ReadAll nil  err:%q", err.Error())
	}
	t.Log("res.StatusCode:" + fmt.Sprintf("%d", res.StatusCode))
	t.Log("body:" + string(body))
}



```

